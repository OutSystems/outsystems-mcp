{
  "//": "TEMPLATE ONLY - do not edit this file, and do not point Cursor at it. Cursor reads server registrations from ~/.cursor/mcp.json (global) or .cursor/mcp.json (project); when this ships inside an installed plugin it is read-only. Copy the block below into one of those two files, creating it if absent, and replace <my-tenant> with the tenant hostname (e.g. mycompany.outsystems.dev). Key: mcpServers (not servers). No type field needed. Never import or migrate an entry from another assistant's MCP config.",
  "//oauth": "OAuth happens via `agent mcp login outsystems` command. Server may need approval via `agent mcp enable outsystems` first.",
  "mcpServers": {
    "outsystems": {
      "url": "https://<my-tenant>/mcp"
    }
  }
}
