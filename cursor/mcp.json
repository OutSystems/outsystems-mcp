{
  "//": "TEMPLATE ONLY - this is not your MCP config. Your server registration belongs in ~/.cursor/mcp.json (global) or .cursor/mcp.json (project); when this file ships inside an installed plugin it is read-only, so do not edit it there. Copy the block below into one of those two files, creating it if absent, and replace <my-tenant> with the tenant hostname (e.g. mycompany.outsystems.dev). Key: mcpServers (not servers). Omit type - Cursor infers the transport from url; never use streamable-http. Never import or migrate an entry from another assistant's MCP config.",
  "//oauth": "OAuth happens via `agent mcp login outsystems` command. Server may need approval via `agent mcp enable outsystems` first.",
  "mcpServers": {
    "outsystems": {
      "url": "https://<my-tenant>/mcp"
    }
  }
}
